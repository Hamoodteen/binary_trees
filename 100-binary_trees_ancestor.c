#include "binary_trees.h"

/**
 * binary_tree_ancestor - f
 * @first: first
 * @second: second
 * Return: bt
*/
binary_tree_t *binary_trees_ancestor(const binary_tree_t *first,
	const binary_tree_t *second)
{
	(void)first;
	(void)second;
	return (NULL);
}
