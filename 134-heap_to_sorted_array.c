#include "binary_trees.h"

/**
 * heap_to_sorted_array - f
 * @heap: heap
 * @size: size
 * Return: int
*/
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	(void)heap;
	(void)size;
	return (0);
}
